Flask>=3.0.0
ollama>=0.2.0
python-dotenv
# Add embedding library (e.g., sentence-transformers)
# sentence-transformers
# Add DB drivers if needed
# Add any other dependencies for core or api service 
requests
torch
TTS
duckdb
duckduckgo-search
pytest
requests-mock # For testing Ollama interactions 
playsound==1.2.2 
# Speech-to-Text dependencies
faster-whisper>=1.1.0
flask-socketio>=5.3.0
python-socketio>=5.9.0
soundfile>=0.12.1
sounddevice>=0.4.6  # For direct audio capture like old system
psutil>=5.9.0  # For system optimization and performance monitoring 
scikit-learn>=1.3.0  # For STM cosine similarity
aiortc>=1.5.0
av>=10.0.0
numpy>=1.23.0
Pillow>=10.0.0  # For vision processing image conversion
